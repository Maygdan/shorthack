# 🔐 Учетные данные для входа

## Тестовые пользователи

### 👨‍🎓 Студент
```
Username: student
Password: student123
Email: student@test.com
```

**Возможности:**
- Просмотр доступных мероприятий
- Участие в квизах и мини-играх
- Получение баллов за выполнение заданий
- Оставление отзывов

---

### 👔 Менеджер
```
Username: manager
Password: manager123
Email: manager@test.com
```

**Возможности:**
- Создание мероприятий
- Просмотр аналитики по мероприятиям
- Управление мероприятиями
- Просмотр обратной связи от студентов

---

## Существующие пользователи

В базе данных также есть следующие пользователи:

1. **Al** (alesharet4253@gmail.com) - Студент
2. **123** (test@mail.ru) - Студент  
3. **testuser123** (test@test.com) - Студент
4. **1234** (test_email@mail.ru) - Студент

⚠️ **Внимание:** Для этих пользователей нужны их оригинальные пароли, которые были установлены при регистрации.

---

## Быстрый вход

### Через веб-интерфейс

1. Откройте http://localhost:5174/
2. Нажмите "Login" или перейдите на http://localhost:5174/login
3. Введите учетные данные:
   - **Для студента:** `student` / `student123`
   - **Для менеджера:** `manager` / `manager123`

### Через API (для тестирования)

**Вход студента:**
```bash
curl -X POST http://localhost:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"student","password":"student123"}'
```

**Вход менеджера:**
```bash
curl -X POST http://localhost:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"manager","password":"manager123"}'
```

---

## Регистрация нового пользователя

Если хотите зарегистрировать нового пользователя:

1. Перейдите на http://localhost:5174/register
2. Заполните форму:
   - Username (имя пользователя)
   - Email
   - User Type (Тип пользователя): **Student** или **Manager**
   - Password (пароль)
3. Нажмите "Register"
4. Вы автоматически войдете в систему

---

## Решение проблем

### "Invalid credentials"

Если вы видите ошибку "Invalid credentials", попробуйте:

1. **Проверьте правильность данных:**
   - Username: `student` (строчными буквами)
   - Password: `student123` (точно как указано)

2. **Используйте тестовые учетные данные:**
   ```
   student / student123
   manager / manager123
   ```

3. **Зарегистрируйте нового пользователя:**
   - Перейдите на страницу регистрации
   - Создайте новый аккаунт
   - После регистрации вы автоматически войдете

### Сброс пароля

Если вы забыли пароль существующего пользователя, обратитесь к администратору или создайте нового пользователя.

---

## Для разработчиков

### Создание пользователя через Django shell

```bash
cd A_back
source ../env_macos/bin/activate
python manage.py shell
```

```python
from mini.models import CustomUser

# Создание студента
student = CustomUser.objects.create_user(
    username='новый_студент',
    email='student@example.com',
    password='пароль123',
    user_type='STUDENT'
)

# Создание менеджера
manager = CustomUser.objects.create_user(
    username='новый_менеджер',
    email='manager@example.com',
    password='пароль123',
    user_type='MANAGER'
)
```

### Проверка существующих пользователей

```python
from mini.models import CustomUser

# Список всех пользователей
for user in CustomUser.objects.all():
    print(f"{user.username} - {user.user_type} - {user.email}")
```

### Изменение пароля

```python
from mini.models import CustomUser

user = CustomUser.objects.get(username='student')
user.set_password('новый_пароль')
user.save()
```

---

## Важные ссылки

- **Frontend:** http://localhost:5174/
- **Backend API:** http://localhost:8000/api/
- **Login:** http://localhost:5174/login
- **Register:** http://localhost:5174/register
- **Events (Студенты):** http://localhost:5174/events
- **Analytics (Менеджеры):** http://localhost:5174/analytics

---

**Последнее обновление:** 29 ноября 2025  
**Версия:** 1.0

