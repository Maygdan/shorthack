# Дизайн-система X5 Group

## 🎨 Корпоративная идентичность

Дизайн платформы полностью соответствует фирменному стилю [X5 Group](https://www.x5.ru/ru/press-center/corporate-identity/).

---

## 📊 Цветовая палитра

### Основные цвета

```css
--x5-green: #00A650;         /* Основной зеленый */
--x5-green-dark: #008C43;    /* Темно-зеленый */
--x5-green-darker: #006E34;  /* Самый темный зеленый */
--x5-green-light: #E8F5E9;   /* Светло-зеленый */
--x5-green-lighter: #F1F9F3; /* Очень светлый зеленый */
```

### Нейтральные цвета

```css
--x5-black: #1A1A1A;         /* Основной черный */
--x5-gray-dark: #424242;     /* Темно-серый */
--x5-gray: #6B6B6B;          /* Средний серый */
--x5-gray-light: #F5F5F5;    /* Светло-серый */
--x5-gray-lighter: #FAFAFA;  /* Очень светлый серый */
--x5-white: #FFFFFF;         /* Белый */
```

### Акцентные цвета

```css
--x5-red: #E53935;           /* Для ошибок */
--x5-blue: #1976D2;          /* Для информации */
--x5-orange: #FF9800;        /* Для предупреждений */
```

---

## 🔤 Типографика

### Шрифт

**Inter** - основной шрифт платформы (с fallback на системные шрифты)

```css
--font-primary: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
```

### Размеры заголовков

| Элемент | Размер | Вес | Использование |
|---------|--------|-----|---------------|
| h1 | 64px | 800 | Главные заголовки hero секций |
| h2 | 48px | 800 | Заголовки секций |
| h3 | 32px | 700 | Подзаголовки |
| h4 | 28px | 700 | Заголовки карточек |
| Body | 16px | 400-500 | Основной текст |

---

## 🎯 Компоненты

### Кнопки

#### Основная кнопка (Primary)
- Градиент: `#00A650` → `#008C43`
- Тень при наведении: зеленая с размытием
- Трансформация: `translateY(-2px)`
- Используется для главных действий

```jsx
<button className="btn btn-primary">Начать</button>
```

#### Вторичная кнопка (Secondary)
- Прозрачный фон
- Граница: 2px solid #00A650
- Цвет текста: #00A650
- При наведении: заливка зеленым

```jsx
<button className="btn btn-secondary">Отмена</button>
```

### Карточки

#### Стандартная карточка
- Граница: 2px solid #F5F5F5
- Радиус: 16px (var(--radius-xl))
- Тень: мягкая (var(--shadow-sm))
- При наведении:
  - Поднимается: `translateY(-6px)`
  - Зеленая граница
  - Увеличенная тень

#### Верхняя полоса
Анимированная зеленая полоса появляется при наведении (4px высотой)

---

## 💫 Анимации

### Переходы

```css
--transition-fast: all 0.15s cubic-bezier(0.4, 0, 0.2, 1);
--transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
--transition-slow: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
```

### Ключевые анимации

**slideUp** - появление элементов снизу вверх
```css
@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

**fadeIn** - плавное появление
```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

---

## 🔍 Тени

```css
--shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.08);
--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.12);
--shadow-xl: 0 16px 48px rgba(0, 0, 0, 0.15);
--shadow-green: 0 4px 14px rgba(0, 166, 80, 0.25);
```

---

## 📐 Радиусы скругления

```css
--radius-sm: 4px;      /* Мелкие элементы */
--radius-md: 8px;      /* Кнопки, инпуты */
--radius-lg: 12px;     /* Карточки среднего размера */
--radius-xl: 16px;     /* Большие карточки */
--radius-full: 9999px; /* Круглые элементы */
```

---

## 🎨 Градиенты

### Основной градиент (кнопки, акценты)
```css
background: linear-gradient(135deg, #00A650 0%, #008C43 100%);
```

### Фоновый градиент (светлый)
```css
background: linear-gradient(135deg, #F1F9F3 0%, #E8F5E9 50%, #FFFFFF 100%);
```

### Градиент для текста
```css
background: linear-gradient(135deg, #00A650 0%, #008C43 100%);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
```

---

## 📱 Адаптивность

### Breakpoints

| Размер | Значение | Использование |
|--------|----------|---------------|
| Mobile | 0-768px | Телефоны |
| Tablet | 769-1024px | Планшеты |
| Desktop | 1025px+ | Компьютеры |

### Примеры

```css
/* Mobile */
@media (max-width: 768px) {
  .home-title { font-size: 32px; }
}

/* Tablet */
@media (max-width: 1024px) {
  .home-title { font-size: 42px; }
}

/* Desktop */
@media (min-width: 1025px) {
  .home-title { font-size: 64px; }
}
```

---

## 🔖 Логотип X5

### Компонент X5Logo

```jsx
import X5Logo from '../components/X5Logo';

// Размеры: small, medium, large
// Цвета: green, white, black
<X5Logo size="medium" color="green" />
```

### Характеристики логотипа

- Шрифт: очень жирный (800)
- Межбуквенное расстояние: -1.5px
- Вертикальная полоса слева (градиент)
- Высота полосы зависит от размера

---

## 📋 Формы

### Поля ввода

```css
.form-input {
  padding: 16px 20px;
  border: 2px solid #F5F5F5;
  border-radius: 8px;
  font-size: 16px;
}

.form-input:focus {
  border-color: #00A650;
  background-color: #F1F9F3;
  box-shadow: 0 0 0 4px rgba(0, 166, 80, 0.08);
}
```

### Select (выпадающий список)

- Кастомная стрелка (зеленая SVG)
- Анимация при фокусе
- Изменение цвета стрелки при фокусе

---

## ✨ Интерактивные эффекты

### Наведение на карточки

1. Подъем вверх (`translateY(-6px)`)
2. Увеличение тени
3. Зеленая граница
4. Светло-зеленый фон
5. Анимация верхней полосы

### Клик по кнопке

1. Градиент темнеет
2. Опускается вниз (`translateY(0)`)
3. Тень уменьшается

---

## 🎯 Принципы использования

### Цвета

- **Зеленый** (#00A650) - для акцентов, кнопок, активных состояний
- **Черный** (#1A1A1A) - для основного текста и заголовков
- **Серый** (#6B6B6B) - для вспомогательного текста
- **Светлый** (#F5F5F5) - для фонов и разделителей

### Иерархия

1. **Primary actions** - зеленые с градиентом
2. **Secondary actions** - с зеленой границей
3. **Tertiary actions** - только текст с зеленым цветом

### Пространство

- Используйте щедрые отступы (40-60px для секций)
- Группируйте связанные элементы (gap: 16-32px)
- Дышащий интерфейс с достаточным whitespace

---

## 📚 Референсы

- [X5 Group - Фирменный стиль](https://www.x5.ru/ru/press-center/corporate-identity/)
- [X5 Group - Брендбук](https://www.x5.ru/upload/medialibrary/69b/v3hhc29zgzj84zoxwnywkufkqy0zvufm.pdf)

---

**Версия:** 1.0  
**Дата обновления:** 29 ноября 2025  
**Разработано:** в соответствии с корпоративной идентичностью X5 Group

