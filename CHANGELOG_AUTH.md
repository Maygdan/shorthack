# Changelog - Исправления системы аутентификации

## Дата: 29 ноября 2025

### 🔧 Исправлено

#### Backend (`A_back/mini/views.py`)
1. **LoginView**:
   - ✅ Добавлен `permission_classes = [AllowAny]` для доступа без авторизации
   - ✅ Изменен формат ответа с `token: {access, refresh}` на `access, refresh` на верхнем уровне
   - ✅ Теперь возвращает токены в правильном формате для frontend

2. **RegisterView**:
   - ✅ Изменен формат ответа с `token: {access, refresh}` на `access, refresh` на верхнем уровне
   - ✅ Теперь возвращает токены в правильном формате для frontend
   - ✅ Пользователь автоматически получает токены после регистрации

#### Backend (`A_back/mini/models.py`)
3. **Сигналы профилей**:
   - ✅ Исправлен `save_user_profile` сигнал - добавлена проверка `created`
   - ✅ Добавлена проверка наличия профилей с помощью `hasattr()`
   - ✅ Добавлена обработка исключений `DoesNotExist`
   - ✅ Используется `get_or_create` вместо `create` для избежания дубликатов

#### Frontend (`A_front/front/src/components/Form.jsx`)
4. **Обработка токенов при логине**:
   - ✅ Изменено с `res.data.token.access` на `res.data.access`
   - ✅ Изменено с `res.data.token.refresh` на `res.data.refresh`

5. **Обработка токенов при регистрации**:
   - ✅ Теперь после регистрации токены сохраняются в localStorage
   - ✅ Пользователь автоматически входит в систему после регистрации
   - ✅ Перенаправление на главную страницу вместо страницы логина

6. **Добавлено поле выбора типа пользователя**:
   - ✅ Добавлен `select` для выбора `STUDENT` или `MANAGER`
   - ✅ Значение по умолчанию: `STUDENT`
   - ✅ Отправляется в запросе регистрации как `user_type`

7. **Улучшена обработка ошибок**:
   - ✅ Добавлена обработка `error.response.data.error`
   - ✅ Улучшены сообщения об ошибках

8. **Навигация**:
   - ✅ Добавлена кнопка перехода на страницу регистрации из логина
   - ✅ Добавлена кнопка перехода на страницу логина из регистрации

#### Frontend (`A_front/front/src/styles/Form.css`)
9. **Улучшен дизайн формы**:
   - ✅ Добавлен стиль для `select.form-input` с кастомной стрелкой
   - ✅ Добавлен стиль `.error-message` для отображения ошибок
   - ✅ Добавлен стиль `.link-button` для кнопок-ссылок

### ✨ Новые возможности

1. **Автоматический вход после регистрации**
   - Пользователь больше не нужно входить отдельно после регистрации
   - Токены сохраняются автоматически

2. **Выбор типа пользователя при регистрации**
   - Можно зарегистрироваться как Студент или Менеджер
   - Автоматически создается соответствующий профиль

3. **Улучшенная навигация**
   - Легко переключаться между формами логина и регистрации

### 🧪 Тестирование

Проведено тестирование API:
- ✅ Регистрация нового пользователя работает корректно
- ✅ Возвращаются правильные токены (access и refresh)
- ✅ Логин с правильными данными работает
- ✅ Логин с неправильными данными возвращает ошибку 401
- ✅ Автоматически создаются профили для студентов и менеджеров

### 📝 Формат ответов API

**Регистрация и логин возвращают:**
```json
{
  "user": {
    "id": 1,
    "username": "testuser",
    "email": "test@test.com",
    "user_type": "STUDENT",
    "phone": "",
    "university": "",
    "telegram_id": "",
    "interests": ""
  },
  "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### 🚀 Статус серверов

- ✅ Backend (Django): http://localhost:8000/
- ✅ Frontend (React + Vite): http://localhost:5174/

### 📚 Документация

Создана документация API аутентификации: `AUTH_API.md`

---

**Все изменения протестированы и работают корректно!**

