# ShortHack Platform — README

Web-платформа для организации и участия в студенческих мероприятиях от X5 Tech.  
Позволяет менеджерам создавать квизы, мини-игры и квесты, а студентам — проходить их, получать баллы, оставлять обратную связь и отслеживать свою статистику.

---

## 🛠️ Технологии

- **Бэкенд**: Django + Django REST Framework + JWT аутентификация
- **Фронтенд**: React + Vite + Axios
- **База данных**: SQLite (по умолчанию, легко заменяется на PostgreSQL/MySQL)
- **Аутентификация**: `djangorestframework-simplejwt`
- **CORS**: `django-cors-headers`

---

## 📦 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd <папка-проекта>
```

### 2. Настройка бэкенда (Django)

#### 2.1. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS / Linux
python3 -m venv venv
source venv/bin/activate
```

#### 2.2. Установка зависимостей

Создайте файл `A_back/requirements.txt`:

```txt
Django==5.2.*
djangorestframework==3.15.*
djangorestframework-simplejwt==5.3.*
django-cors-headers==4.4.*
python-dotenv
```

Установите зависимости:

```bash
cd A_back
pip install -r requirements.txt
```

#### 2.3. Настройка переменных окружения

Создайте файл `.env` в папке `A_back/`:

```env
DEBUG=True
SECRET_KEY=ваш-секретный-ключ-для-django
```

> 💡 Секретный ключ можно сгенерировать:  
> ```python
> from django.core.management.utils import get_random_secret_key
> print(get_random_secret_key())
> ```

#### 2.4. Миграции и создание суперпользователя

```bash
# Применить миграции
python manage.py makemigrations
python manage.py migrate

# Создать суперпользователя (опционально)
python manage.py createsuperuser
```

#### 2.5. Запуск сервера

```bash
python manage.py runserver
```

> 📌 Бэкенд будет доступен по адресу: `http://localhost:8000`

---

### 3. Настройка фронтенда (React + Vite)

#### 3.1. Установка зависимостей

```bash
cd A_front
npm install
```

#### 3.2. Настройка переменных окружения

Создайте файл `.env` в папке `A_front/`:

```env
VITE_API_URL=http://localhost:8000/api/
```

> Убедитесь, что URL совпадает с адресом вашего Django-сервера.

#### 3.3. Запуск фронтенда

```bash
npm run dev
```

> 📌 Фронтенд будет доступен по адресу: `http://localhost:5173`

---

## 🧪 Примеры использования

### Регистрация
- **Студент**:  
  - Имя пользователя: `student1`  
  - Пароль: `pass1234`  
  - Email: `student1@example.com`  
  - Тип: `STUDENT`

- **Менеджер**:  
  - Имя пользователя: `manager1`  
  - Пароль: `pass1234`  
  - Email: `manager1@x5tech.ru`  
  - Тип: `MANAGER`

### Возможности
- **Студенты**:
  - Просматривают активные мероприятия
  - Проходят квизы и мини-игры
  - Получают баллы
  - Оставляют обратную связь
  - Видят свою статистику

- **Менеджеры**:
  - Создают мероприятия (квизы, мини-игры)
  - Назначают баллы за прохождение
  - Просматривают аналитику:
    - Сколько студентов посмотрели мероприятие
    - Сколько завершили
    - Средний балл
    - Обратная связь

---

## 📁 Структура проекта

```
.
├── A_back/                  # Django-бэкенд
│   ├── mini/                # Основное приложение
│   │   ├── models.py        # Модели: CustomUser, Event, Quiz и т.д.
│   │   ├── serializers.py   # Сериализаторы
│   │   ├── views.py         # API-представления
│   │   ├── urls.py          # Маршруты API
│   │   └── ...
│   ├── backend/             # Настройки Django
│   │   └── settings.py      # Конфигурация (включая JWT и CORS)
│   ├── manage.py
│   └── .env                 # Переменные окружения
│
└── A_front/                 # React-фронтенд
    ├── src/
    │   ├── api.js           # Axios-инстанс с JWT
    │   ├── components/
    │   ├── pages/
    │   ├── App.jsx          # Роутинг
    │   └── ...
    ├── .env                 # URL бэкенда
    └── package.json






## 📬 Поддержка

По всем вопросам обращайтесь к команде разработки ShortHack.   🚀